0) треугольник, глубина рекурсии
1) массив вершин - изначально их три
запуск функции, на вход: массив вершин текущий  глубин рекурсии
	0) если глубина = 0, то
		рисеуем и возвращаемся
	   иначе
		идём дальше
	1) переданный массив вершин копируется
	   поворачивается на 90 градусов
	   сжимается по Х в 1/3 раз
	   возвращаются новые координаты
	2) переданный массив вершин копируется
	   поворачивается на -90 градусов
	   сжимается по Х в 1/3 раз
	   возвращаются новые координаты
	3) переданный массив вершин (первоначальный)
	   сжимается в 3 раза по X и Y
	4) массивы из 1) и 2) и 3) сливаются в один
	5) рекурсивно возвращаемся в 0) с глубиной -1